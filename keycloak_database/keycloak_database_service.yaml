apiVersion: v1
kind: Service
metadata:
  name: keycloak-database-service
  namespace: smart-search
spec:
  selector:
    app: keycloak-database
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP
